content = """# 🧠 The Ultimate Productivity Goon Cave: App Workflow & Dev Blueprint

Ayo, what's good chief. We boutta lock in and build the most based, W-rizz productivity app the internet has ever seen. No cap, this is gonna be the ultimate goon cave for your thoughts, completely mewing on mid apps like Notion. We taking that Milanote inspo and giving it that skibidi Gen Z glow-up. 

Here is the absolute gigachad workflow, dependency list, and the JSON prompt to feed your Cursor IDE so it can spit out pure fire.

---

## 🗺️ The Workflow (Edging the Infinite Canvas)

This app is all about spatial organization. You don't just write notes; you construct a mental goon cave.

### 1. The Lobby (Dashboard)
* **Vibe:** Dark mode only (we don't do flashbangs here). 
* **Action:** You see your recent "Boards". Hit "Create New Board" to enter the zone.

### 2. The Infinite Edge (The Canvas)
* **Vibe:** An infinite 2D grid where you can pan and zoom like a true sigma.
* **Action:** Right-click or use a floating sidebar to drop "Blocks" onto the canvas. 

### 3. The Blocks (The Gyatts of the App)
Based on your inspo pic, we need modular, draggable nodes.
* **Heading/Text Block:** Standard markdown text.
* **To-Do Block:** Checklists for your daily mewing streaks and physical stats (like the "Need to work on" box with Shoulder, Chest, etc.).
* **Table Block:** Mini spreadsheets for tracking stats (like the Date/Height/Weight table in the pic).
* **Social Link Block:** Your "quick link" feature. Paste a Twitter/X, Insta, or TikTok link, and it unfurls into a clean card.

### 4. The Rizz Lines (Connections)
* **Action:** Click and drag from one block to another to draw an arrow. This links related concepts so your brainrot actually makes sense.

---

## 📦 The Stack & Dependencies (The Sigma Toolkit)

To build this without pulling your hair out, we are using the most based modern React stack.

### Core Stack
* **Framework:** `next` (Next.js 14 App Router) - Server components for that fast TTFB.
* **Language:** TypeScript (Strict mode, we don't do any `any` type cap).
* **Styling:** `tailwindcss` - Utility first, no CSS headache.
* **UI Components:** `shadcn-ui` & `radix-ui` - Accessible, copy-paste components.
* **Icons:** `lucide-react` - Clean AF.

### The Secret Sauce (Gooner Essentials)
* **Canvas & Nodes:** `reactflow` (Absolute goated library for infinite canvas and node connecting).
* **State Management:** `zustand` (Redux is for boomers, Zustand is lightweight and bussin).
* **Database & Auth:** `@supabase/supabase-js` (Postgres backend, easy OAuth for socials).
* **Drag & Drop (if not using ReactFlow for sidebars):** `@dnd-kit/core`

---

## 🤖 Cursor IDE Build Prompt (JSON Format)

Copy this JSON into Cursor's Composer or AI chat to get it to build the scaffolding for you. It's detailed AF so the AI knows exactly how to lock in.